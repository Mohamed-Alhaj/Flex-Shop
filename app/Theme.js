
import { createTheme } from "@mui/material/styles";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#530840",
    },
    secondary: {
      main: "#6e38618a",
    },
    background:{
      default:"#f386e48f"
    }
  },
});

export default Theme;
